version: 2.1
jobs:
  rspec_and_rubocop:
    working_directory: ~/app
    executor:
      name: tests/default
      tag: 2.7.2
      test_db_name: app_name_test
    steps:
      - ruby-tests/setup-and-run-tests:
        bundler-version: 2.2.4

  heroku_deploy:
    executor: heroku/default
    steps:
      - checkout
      - heroku/install
      - run:
          command: >
            echo "The command above installs Heroku, the command below deploys.
            What you do inbetween is up to you!"
      - heroku/deploy-via-git

# orbs:jobsのパッケージ
orbs:
  heroku: circleci/heroku@1.2.4
  ruby-tests: zfhui/ruby-tests@0.0.2

# workflows:jobsの実行順番
workflows:
  version: 2.1
  build:
    jobs:
      - rspec_and_rubocop
      - heroku_deploy:
          requires:
            - rspec_and_rubocop
